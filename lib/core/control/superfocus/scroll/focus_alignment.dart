enum FocusAlignment {
  /// 默认行为：仅在焦点越出视口安全边界时，进行最小幅度的滚动（即“一个接一个推着走”）。
  keepVisible,

  /// 强制将该节点滚动到视口/安全区的正中心。
  center,

  /// 强制将该节点对齐到视口/安全区的最顶端。
  top,

  /// 强制将该节点对齐到视口/安全区的最底端。
  bottom,

  /// 强制视口滚动到绝对起点（无论节点本身坐标在哪，获取焦点时页面强制回到最顶部）。
  viewportStart,
}
