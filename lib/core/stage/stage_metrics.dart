/// Standard metrics for the main content area (The Stage).
class StageMetrics {
  const StageMetrics._();

  /// Horizontal units from the sidebar/right-edge.
  static const double paddingHorizontalU = 6.0;

  /// Vertical units from the top/bottom edges.
  static const double paddingVerticalU = 8.0;

  /// Corner radius for the stage content if needed.
  static const double borderRadiusU = 4.0;
}
