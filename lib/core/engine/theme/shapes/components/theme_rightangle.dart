import 'package:flutter/material.dart';
import '../theme_shapes.dart';

final rightAngleShapeLayer = ThemeShapeLayer(
  sidebar: const RoleShape(radius: BorderRadius.zero, concaveRadius: 0),
  card: const RoleShape(radius: BorderRadius.zero),
  appBackground: const RoleShape(radius: BorderRadius.zero),
  button: const RoleShape(radius: BorderRadius.zero),
);
